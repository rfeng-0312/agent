# 名侦探作业帮 - 项目文件存放规则

## 📁 .claude 文件夹结构

```
.claude/
├── rules/
│   ├── file-organization.md          # 文件存放规则（本文件）
│   ├── testing-guidelines.md         # 测试代码管理规则
│   └── development-standards.md      # 开发规范
├── tests/
│   ├── temporary/                    # 临时测试文件（自动清理）
│   ├── integration/                  # 集成测试
│   └── performance/                  # 性能测试
├── docs/
│   ├── api/                          # API文档
│   ├── deployment/                   # 部署文档
│   └── user-guide/                   # 用户指南
├── scripts/
│   ├── cleanup.py                    # 测试文件清理脚本
│   ├── validate.py                   # 文件结构验证脚本
│   └── deploy-check.py               # 部署前检查脚本
└── templates/
    ├── test-template.py              # 测试代码模板
    └── doc-template.md               # 文档模板
```

## 🗂️ 项目根目录文件存放规则

### 必需的根目录文件
- `README.md` - 项目主要说明文档
- `requirements.txt` - Python依赖列表
- `.env.example` - 环境变量模板
- `.gitignore` - Git忽略文件
- `LICENSE` - 开源协议

### 配置文件
- `docker-compose.yml` - Docker编排配置
- `Dockerfile` - Docker镜像配置
- `nginx.conf` - Nginx配置（如果使用）
- `deploy.sh` / `deploy.bat` - 部署脚本

### 源代码目录
- `src/` - 所有源代码
  - `app.py` - Flask主应用
  - `prompts.py` - 提示词配置
  - `utils/` - 工具函数
  - `models/` - 数据模型
  - `api/` - API路由

### 前端资源
- `frontend/` - 前端文件
  - `templates/` - HTML模板
  - `static/` - 静态资源（CSS、JS、图片）
  - `components/` - 前端组件（如果使用）

### 数据目录
- `data/` - 运行时数据
  - `uploads/` - 用户上传文件
  - `sessions/` - 会话数据
  - `logs/` - 日志文件

### 测试目录
- `tests/` - 正式测试代码
  - `unit/` - 单元测试
  - `integration/` - 集成测试
  - `e2e/` - 端到端测试

## 🧪 测试代码管理规则

### 临时测试文件
1. **存放位置**：`.claude/tests/temporary/`
2. **命名规范**：`test_<功能>_<时间戳>.py`
   - 示例：`test_api_20251206_164500.py`
3. **自动清理**：
   - 测试成功运行后立即自删除
   - 使用 `try/finally` 确保清理
   - 失败的测试保留24小时后自动清理

### 测试代码模板
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
临时测试文件 - 自动删除
测试功能：[简要描述]
创建时间：{timestamp}
"""

import os
import sys
import time

def cleanup_test_file():
    """测试结束后自删除"""
    try:
        os.remove(__file__)
        print(f"✓ 测试文件已删除: {__file__}")
    except Exception as e:
        print(f"✗ 删除失败: {e}")

def main():
    try:
        # 测试代码
        print("开始测试...")
        # ... 测试逻辑 ...
        print("✓ 测试通过")
        return True
    except Exception as e:
        print(f"✗ 测试失败: {e}")
        return False
    finally:
        cleanup_test_file()

if __name__ == "__main__":
    success = main()
    sys.exit(0 if success else 1)
```

### 测试文件清理脚本
创建 `.claude/scripts/cleanup.py`：
- 每小时运行一次
- 清理超过24小时的临时测试文件
- 清理空目录
- 生成清理报告

## 📚 文档存放规范

### .claude/docs/ 目录结构
```
docs/
├── api/                    # API相关文档
│   ├── endpoints.md       # API端点说明
│   ├── authentication.md  # 认证机制
│   └── errors.md          # 错误代码
├── deployment/            # 部署相关文档
│   ├── docker.md         # Docker部署
│   ├── nginx.md          # Nginx配置
│   └── production.md     # 生产环境部署
├── development/           # 开发相关文档
│   ├── setup.md          # 开发环境搭建
│   ├── contributing.md   # 贡献指南
│   └── debugging.md      # 调试指南
└── user-guide/           # 用户指南
    ├── getting-started.md # 快速开始
    ├── features.md        # 功能介绍
    └── faq.md            # 常见问题
```

### 文档命名规范
1. 使用小写字母和连字符：`feature-name.md`
2. 文档标题与文件名对应
3. 包含最后更新日期和作者

### 文档模板
```markdown
# 文档标题

## 概述
[简要说明文档内容]

## 详细内容
[详细描述]

## 示例
[代码示例或使用示例]

---
**最后更新**: 2025-12-06
**作者**: [作者名称]
**版本**: 1.0.0
```

## 🔧 文件组织最佳实践

### 1. 目录深度控制
- 最多不超过4层目录
- 常用功能不超过3层访问

### 2. 文件命名
- 使用有意义的英文名称
- 避免中文文件名（路径问题）
- 配置文件使用`.env`, `.config`, `.json`后缀

### 3. 版本控制
- 只追踪源代码和配置文件
- 忽略临时文件、日志、用户数据
- 使用`.gitignore`管理忽略规则

### 4. 安全考虑
- 敏感配置使用环境变量
- API密钥不提交到版本控制
- 用户上传文件隔离存放

## 📋 检查清单

在提交代码或部署前，确保：
- [ ] 临时测试文件已清理
- [ ] 文档已更新并放在正确位置
- [ ] 敏感信息已移除或加密
- [ ] 文件结构符合规范
- [ ] 测试都已通过

## 🚀 自动化工具

### 1. 文件结构验证
```bash
python .claude/scripts/validate.py
```

### 2. 清理临时文件
```bash
python .claude/scripts/cleanup.py
```

### 3. 部署前检查
```bash
python .claude/scripts/deploy-check.py
```

---

**规则版本**: 1.0.0
**生效日期**: 2025-12-06
**维护者**: 项目团队