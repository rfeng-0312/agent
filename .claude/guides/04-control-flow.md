# 第4课：条件判断和循环

## 🎯 学习目标
- 学会使用if判断
- 掌握for循环
- 理解while循环
- 让程序可以做出选择和重复

## 🛤️ 条件判断 - if语句

编程中最重要的事情之一就是做决定！如果...就...

### 基本的if语句

```python
age = 18

if age >= 18:
    print("你已经成年")
    print("可以独自看电影了")
```

### if-else - 两种选择

```python
age = 16

if age >= 18:
    print("你已经成年")
else:
    print("你还未成年")
```

### if-elif-else - 多种选择

```python
score = 85

if score >= 90:
    print("优秀")
elif score >= 80:
    print("良好")
elif score >= 60:
    print("及格")
else:
    print("需要努力")
```

## 🔍 条件判断的操作符

| 操作符 | 含义 | 例子 |
|--------|------|------|
| == | 等于 | age == 18 |
| != | 不等于 | age != 18 |
| > | 大于 | age > 18 |
| < | 小于 | age < 18 |
| >= | 大于等于 | age >= 18 |
| <= | 小于等于 | age <= 18 |

### 多个条件

```python
age = 20
has_ticket = True

# and - 两个条件都为真
if age >= 18 and has_ticket:
    print("可以进入电影院")

# or - 只要一个条件为真
if age < 18 or has_ticket == False:
    print("不能进入")
```

## 🔄 for循环 - 重复执行固定次数

当你知道要重复多少次时，用for循环：

```python
# 打印5次"你好"
for i in range(5):
    print("你好")

# 遍历列表
fruits = ["苹果", "香蕉", "橙子"]
for fruit in fruits:
    print("我喜欢吃：" + fruit)

# 带编号的循环
for i in range(1, 6):  # 1到5
    print(f"第{i}次循环")
```

## ⏰ while循环 - 重复执行直到条件不满足

当你不知道要重复多少次时，用while循环：

```python
count = 0
while count < 5:
    print(f"计数：{count}")
    count = count + 1  # 重要：改变条件，否则无限循环！
```

### 实际例子：猜数字游戏

```python
secret = 7
guess = 0

while guess != secret:
    guess = int(input("猜一个数字（1-10）："))

    if guess < secret:
        print("太小了！")
    elif guess > secret:
        print("太大了！")
    else:
        print("恭喜你猜对了！")
```

## 🎮 综合练习

```python
# 打印乘法表
for i in range(1, 10):  # 1到9
    for j in range(1, 10):  # 1到9
        result = i * j
        print(f"{i} × {j} = {result}", end="\t")
    print()  # 换行

# 判断闰年
year = 2024

if year % 4 == 0:
    if year % 100 == 0:
        if year % 400 == 0:
            print(f"{year}年是闰年")
        else:
            print(f"{year}年不是闰年")
    else:
        print(f"{year}年是闰年")
else:
    print(f"{year}年不是闰年")
```

## 🏗️ 项目中的实际应用

在我们的项目中：

```python
# 1. 检查文件类型
def allowed_file(filename):
    # 使用in判断是否在集合中
    allowed_extensions = {'png', 'jpg', 'jpeg', 'gif', 'webp'}

    # 检查是否有扩展名
    if '.' not in filename:
        return False

    # 获取扩展名并检查
    extension = filename.rsplit('.', 1)[1].lower()
    if extension in allowed_extensions:
        return True
    else:
        return False

# 2. 处理上传的多个文件
files = ['photo1.jpg', 'doc.pdf', 'image.png']

for file in files:
    if allowed_file(file):
        print(f"{file} 可以上传")
    else:
        print(f"{file} 不能上传")
```

## ✏️ 小练习

1. 写一个程序，输入年龄，判断是婴儿、儿童、青少年、成年还是老年
2. 写一个程序，打印1到100之间所有的偶数
3. 写一个程序，计算1到100的和

```python
# 练习1答案
age = int(input("请输入年龄："))

if age < 3:
    print("婴儿")
elif age < 13:
    print("儿童")
elif age < 18:
    print("青少年")
elif age < 65:
    print("成年")
else:
    print("老年")

# 练习2答案
print("1到100的偶数：")
for num in range(1, 101):
    if num % 2 == 0:
        print(num)

# 练习3答案
total = 0
for num in range(1, 101):
    total = total + num
print(f"1到100的和是：{total}")
```

## 💡 编程技巧

1. **缩进很重要！**
   - Python用缩进表示代码块
   - 通常用4个空格

2. **避免无限循环**
   - while循环一定要有改变条件的语句

3. **使用break提前退出**
```python
for i in range(100):
    if i == 5:
        break  # 退出循环
    print(i)
```

4. **使用continue跳过本次循环**
```python
for i in range(10):
    if i % 2 == 0:
        continue  # 跳过偶数
    print(i)  # 只打印奇数
```

## 🤔 思考题

```python
x = 0
while x < 10:
    x = x + 1
    if x == 5:
        continue
    print(x)
```

**输出是什么？**
```
1
2
3
4
6
7
8
9
10
```
（5被跳过了）

## 🎉 下节课预告

下节课我们将学习：
- 列表 - 存储多个数据
- 字典 - 用键值对存储数据
- 元组和集合

---

**记住：条件判断和循环是编程的灵魂，让程序变得智能！** 🧠

[下一课：列表和字典 →](05-data-structures.md)