# 第3课：函数 - def的魔法

## 🎯 学习目标
- 理解什么是函数
- 学会创建自己的函数
- 知道为什么要使用函数

## 💭 什么是函数？

想象函数是一台机器：
- 你放入材料（参数）
- 机器加工处理
- 输出结果（返回值）

**函数就是把一些代码打包，给它起个名字，以后可以直接使用**

## 🏭 为什么要用函数？

1. **重复使用** - 写一次，用多次
2. **整理代码** - 让代码更清晰
3. **方便修改** - 改一处，处处改

## 📝 创建函数

```python
def say_hello():
    print("你好！")
    print("欢迎使用名侦探作业帮")
    print("祝你学习愉快！")
```

**解释：**
- `def` 是定义函数的关键字
- `say_hello` 是函数的名字
- `()` 是参数列表（后面会讲）
- `:` 表示函数开始
- 缩进的代码是函数内容

## 🚀 使用函数

```python
# 定义函数
def say_hello():
    print("你好！")

# 调用函数（使用函数）
say_hello()  # 输出：你好！
say_hello()  # 又输出一次：你好！
say_hello()  # 再输出一次：你好！
```

## 📦 带参数的函数

函数可以接收输入的数据：

```python
def greet(name):
    print("你好，" + name + "！")

# 使用时传入参数
greet("小明")  # 输出：你好，小明！
greet("小红")  # 输出：你好，小红！
greet("老师")  # 输出：你好，老师！
```

## 🔄 带返回值的函数

函数可以返回结果：

```python
def add(a, b):
    result = a + b
    return result  # 返回结果

# 使用函数的返回值
sum1 = add(3, 5)  # sum1 = 8
sum2 = add(10, 20)  # sum2 = 30

print("3 + 5 = " + str(sum1))
print("10 + 20 = " + str(sum2))
```

## 🎮 实际例子

```python
# 计算圆的面积
def calculate_circle_area(radius):
    pi = 3.14159
    area = pi * radius * radius
    return area

# 使用
r = 5
area = calculate_circle_area(r)
print(f"半径为{r}的圆的面积是：{area}")

# 检查是否成年
def is_adult(age):
    if age >= 18:
        return True
    else:
        return False

age = 16
if is_adult(age):
    print("已经成年")
else:
    print("还未成年")
```

## 🏗️ 项目中的实际应用

在我们的项目中有很多函数：

```python
# 1. 检查文件类型是否允许
def allowed_file(filename):
    return '.' in filename and \
           filename.rsplit('.', 1)[1].lower() in {'png', 'jpg', 'jpeg'}

# 使用
if allowed_file("photo.jpg"):
    print("可以上传")
else:
    print("文件类型不允许")

# 2. 获取学科提示词
def get_subject_prompt(subject):
    if subject == "physics":
        return "你是一个物理老师..."
    elif subject == "chemistry":
        return "你是一个化学老师..."
    else:
        return "你是一个全科老师..."

# 使用
prompt = get_subject_prompt("physics")
print(prompt)
```

## ✏️ 小练习

1. 创建一个函数，打印你的个人信息
2. 创建一个函数，计算两个数的乘积
3. 创建一个函数，判断一个数是奇数还是偶数

```python
# 练习1
def print_info():
    print("姓名：小明")
    print("年龄：18岁")
    print("爱好：编程")

# 练习2
def multiply(a, b):
    return a * b

# 练习3
def check_odd_even(number):
    if number % 2 == 0:
        return "偶数"
    else:
        return "奇数"
```

## 💡 函数的小贴士

1. **函数名要有意义**
   - ❌ `def f1():`
   - ✅ `def calculate_sum():`

2. **一个函数做一件事**
   - 不要在一个函数里做太多事情

3. **添加注释说明**
```python
def convert_temperature(celsius):
    """
    将摄氏度转换为华氏度
    参数：celsius - 摄氏度
    返回：华氏度
    """
    fahrenheit = celsius * 9/5 + 32
    return fahrenheit
```

## 🤔 思考题

```python
def mystery(x):
    return x * 2

result = mystery(mystery(3))
print(result)  # 会输出什么？
```

**答案：12**
- mystery(3) 返回 6
- mystery(6) 返回 12

## 🎉 下节课预告

下节课我们将学习：
- if 条件判断
- for 和 while 循环
- 如何让程序做出选择

---

**记住：函数就像积木，搭建出复杂的程序！** 🧱

[下一课：条件判断和循环 →](04-control-flow.md)